import java.util.regex.*;\n\npublic class CaseInsensitivePalindromeChecker {\n    public static boolean isPalindrome(String input) {\n        // Preprocess the input: remove spaces and convert to lowercase\n        String processedInput = input.replaceAll("\\s+", "").toLowerCase();\n        // Check if it's a palindrome using regex\n        String regex = "^(.)\1*$"; // this will match any string that is a palindrome\n        return Pattern.matches(regex, processedInput);\n    }\n\n    public static void main(String[] args) {\n        String testString = "A man a plan a canal Panama";\n        System.out.println(isPalindrome(testString) ? "Palindrome" : "Not a palindrome");\n    }\n}